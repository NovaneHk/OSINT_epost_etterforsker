// Lead-specific re-export of StatusBadge filtered to LeadStatus values.
// Keeping this as its own file so leads components import from a leads-local path.
export { StatusBadge as LeadStatusBadge } from '@/components/shared/status-badge';
